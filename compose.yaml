services:
  searxng:
    image: docker.io/searxng/searxng:latest
    container_name: pi-search-searxng
    restart: unless-stopped
    ports:
      - "127.0.0.1:8888:8080"
    volumes:
      - "./deploy/searxng-settings.yml:/etc/searxng/settings.yml:ro"
